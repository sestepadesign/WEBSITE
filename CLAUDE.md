# S'Estepa Design — Website

Astro static site for design.sestepa.com (landscape architecture studio, Mallorca). Multi-locale (en/es/de), deployed to Cloudflare Pages.

## Pending Human Action

Things a Claude Code sandbox session cannot do itself — remove each line once actually done, don't just leave it here stale.

- **Delete 5 remote branches** — all already merged into `master`, zero data loss, one click each in the GitHub web UI (Branches tab): `claude/sant-llorenc-botanicals`, `claude/sant-llorenc-new-text-jm-cleanup`, `claude/session-log-2026-08-07`, `preview`, `claude/sant-llorenc-video-update-ffhitf`. A sandbox session can't do this itself — see "Wider team context" below. Added 2026-08-07.

## ABSOLUTE HANDOFF RULE (STATUS.md)

Before modifying any code, **you MUST read `STATUS.md`** in the root directory. 
- You must verify that the local codebase is up-to-date with the checksum in `STATUS.md`. If it is outdated, `git pull` first.
- When finishing your session, you **MUST write your session log** at the top of the "HISTÓRICO DE SESSÕES" section in `STATUS.md`, including your branch/commit hash, to preserve state for the next agent. This is non-negotiable.
- **`STATUS.md` does not exist yet in this repo as of 2026-08-07** (checked full `git log --all -- STATUS.md`: never committed, by anyone). It's being bootstrapped separately — don't treat its absence as an error, and don't invent its contents from scratch without checking with the user first.

## Wider team context (S'Estepa / "1. WEBSITES")

This repo is one of several sites (`sestepa-design`, `ines-grimaux`, `grimaux-net`, `castro-alves`) managed by a small team through a shared Google Drive folder (`G:\Meu Drive\1. WEBSITES\`), edited by multiple people and AI tools (Cursor, Antigravity, Claude Code — listed there as `Claude-Code`). Full protocol: `docs/equipa/INSTRUCOES.md`, `docs/EQUIPA.md`, `docs/ONDE-ESTA-TUDO.md`.

- **The real coordination mechanism is a Drive-only lock file** (`TRABALHANDO.json` per project + `lock_manager.py` / `set_lock_agent.py` scripts living in the Drive folder), not this `STATUS.md` rule above — those appear to be two different, not-fully-reconciled handoff schemes.
- **A GitHub-only clone (no Drive mount) cannot participate in that lock system at all** — confirmed by `docs/equipa/PARA-CLONE-SEM-DRIVE.md`, which documents this exact scenario. A Claude Code session running in a cloud sandbox (like this one, no `G:\` access) can read/edit code and push, but cannot acquire/release the project lock, see other agents' current activity, or know if someone else is mid-edit on the Drive side.
- **Branch policy conflict**: the team's documented protocol (`INSTRUCOES.md`) says work happens *directly on `master`*, no feature branches. This file's own "Git branch hygiene" section below says the opposite (short-lived branch → merge). Not reconciled as of 2026-08-07 — ask the user before assuming either one silently changes.
- Local builds on the Drive/Windows side are known to be unreliable (`.agents/AGENTS.md`: Google Drive sync can silently corrupt `node_modules/astro/bin/astro.mjs`) — doesn't affect a clean Linux sandbox clone like this one.
- **A GitHub-only cloud sandbox session cannot delete remote branches** — `git push origin --delete <branch>` is blocked by the sandbox's egress proxy (`403`, "RPC failed"), consistently, and no available GitHub tool exposes branch deletion either. Confirmed 2026-08-07. Don't retry it — merge (`--no-ff`) and delete the *local* branch as usual, but leave the merged remote branch for a human (or a non-sandboxed session) to delete via the GitHub web UI. Check `git branch -r --merged origin/master` occasionally for a backlog of these to hand off.

## Commands

```
npm install
npm run dev      # local dev server, http://localhost:4321
npm run build    # production build to dist/
```

## Deploy pipeline

Cloudflare Pages is connected to this GitHub repo and auto-deploys on every push to `master`. There is no separate manual deploy step — pushing to `master` is publishing to production.

## Review workflow — depends on the session environment

- **Desktop / interactive session:** run `npm run dev` and let the user click through the change on `localhost:4321` *before* it's pushed anywhere. This is the default approval step whenever a local preview is reachable.
- **Mobile / remote session (no localhost access for the user):** there is no way to hand the user a local preview. In this case: build/verify locally in the sandbox first (build passes, screenshot via Playwright if useful), push to a feature branch, then merge to `master` only with the user's explicit go-ahead. Approval happens *after* the Cloudflare deploy, by the user checking the live URL — not before.

## Git branch hygiene

- `master` is the only long-lived branch.
- Work happens on a short-lived feature branch, gets merged into `master` (`--no-ff`, keep the merge commit), and the feature branch is deleted right after — locally and on `origin`. Don't leave merged branches lying around.
- Don't invent a permanent "preview" branch to hold work between tasks — Cloudflare Pages already gives every non-production branch/PR its own automatic preview deployment, so there's no need to hand-maintain one.

## Content structure

- `src/data/projects.ts` — portfolio project data (one entry per project: slug, images, video, translations).
- `src/data/portfolio-categories.ts` — maps project slugs to portfolio filter categories (`estate`, `residential`, `terrace`, `commercial`, `vertical`).
- `src/data/site-urls.ts` — per-locale URL paths for every page and project (`PROJECT_PUBLIC_PATHS`), legacy slug redirects, default OG images.
- `src/components/site/*Page.astro` — one component per page; thin route files in `src/pages/` just import and render them.
- Every project's images live under `public/portfolio/<slug>/images/`. Convert new photos to `.webp` before adding them — the site's Lighthouse scores (currently ~98/100/96/100) depend on image weight staying low.
- `sant-llorenc` is the flagship project: it's pinned first in the portfolio grid (`PortfolioPage.astro`), and its images are the sitewide default OG image (`Layout.astro`, `servicesPage.astro`). Its hero background video (`/videos/sant_llorenc_hero.mp4` + poster) is also reused directly on the home page — treat that file as shared, not project-specific.

## Session Log (Claude Code)

Newest entry first. Each entry: date, commit hash on `master` at end of session, short summary. This is separate from (and doesn't replace) the team's Drive-side `TRABALHANDO.json` lock — see "Wider team context" above.

- **2026-09-01** (SEO session, part 3 — Services page) — commits `1a29d68`, `71869b7` (direct to `master`). Follow-through on the de-cannibalisation left open by `cadef94`. The Services page (`src/data/services.ts` + `src/components/site/servicesPage.astro`) was a ~300-word near-duplicate of the homepage, both `<h1>`/title "Garden Design … Mallorca". Client asked to *improve* it rather than just retitle ("make it an interesting page for anyone who lands there"). Now: (a) **evocative hero** — single `<h1>` "The garden becomes the favourite room of the house." (ES "El jardín se convierte en el lugar favorito de la casa.", DE "Der Garten wird zum Lieblingsort des Hauses."), own-team / whole-process lead, CTA; background is a Crestatx shot (`heroImage` const), distinct from the homepage hero; `.svc-hero` `align-items:center` + `padding-top:5rem` to clear the fixed header, headline `max-width:22ch`. New `heroHeadline`/`heroLead` fields on `ServicesContent`. (b) **projects strip** — 6 gardens (`stripSlugs`: sant-llorenc, finca-garden-campos-mallorca, jardin-mediterraneo, hotelcabotlasvelas, costadelacalma, terrace-garden-in-palma), reuses `<ProjectCard>`, pure-CSS `.svc-strip-scroll` (scroll-snap on mobile, `repeat(3,1fr)` grid ≥1100px, **no JS**). (c) **title tag** reframed to "…Services in Mallorca" in all 3 locales (`seoTitle`); the old keyword line kept as an `<h2>` (`content.title`); the old `<h1>` in `.page-header` demoted to `<h2>`. Homepage keeps the bare "garden design mallorca" head term to itself. Existing 4 service cards / 12-point list / CTA panel unchanged below. Verified: local `astro build` (Astro 6.4.8, 138 pages, 1 `<h1>`/locale, 6 cards, sitemap 123) + live desktop & mobile screenshots (hero clears header, strip scrolls). **Still open (client sign-off / their content):** homepage "why a Mallorquin studio" block + FAQ link (they have `/faq/`, EN-only, proper FAQPage schema — do NOT duplicate that schema); internal-linking pass (contextual, varied natural anchors, ~1 per project/article page — client OK'd "se não for chato", wants a 3-page sample first).
- **2026-09-01** (SEO session, cont. of the image-sitemap one) — commit `cadef94` (direct to `master`). Client goal, from SERP screenshots: for "garden design mallorca" (+ "landscape design mallorca" etc.) be the #1 real *website* (above mashambadesign.com) and recover the image-pack presence they had until ~Aug. SERP read: S'Estepa already ranks ~#3-4 organic, above Studio Terraforma; the #1 result is the @mallorcagardendesign *Instagram* entity (their site ranks below the map — weak); helencummins is an article that lists S'Estepa. The page that ranks for the term is the **homepage** (ES `/es/` = 1133 words, confirmed), while the thin "Services" page (`src/data/services.ts`, EN 299 / ES 304 words, slug `/garden-design-mallorca-landscape-management-villas-fincas/`) cannibalises it, and the exact-match slug `/garden-design-mallorca/` was 301'ing to that *thin* page. Changes this commit (all reversible, homepage + services page CONTENT untouched — client explicitly said "don't lose the authority we've built"): (1) `_redirects` — `/garden-design-mallorca/` and the old `...mediterranean-style...` orphan now 301 to `/` (homepage) instead of the services page; consolidates equity onto the ranking URL. (2) `Schema.astro` — project `@type` `"LandscapingProject"` (not a real Schema.org type, Google discards the block; flagged in the 27/08 Antigravity audit, never applied) → `"CreativeWork"`. (3) `astro.config.mjs` — new `ALIAS_PATHS` (the 9 `PROJECT_PATH_ALIASES` legacy slugs) added to the sitemap `EXCLUDE` set; `sitemap-0.xml` went 132→123 URLs, all the 301/foreign-canonical entries gone, redirects themselves untouched. (4) deleted stray `src/utils/schema.ts.bak-2026-07-03`. Verified live post-deploy: `/garden-design-mallorca/` → 301 → `/`; sitemap 123 URLs, 0 aliases; project pages emit `CreativeWork`. **Still open (needs client's brand-voice sign-off, drafts proposed, NOT done):** de-cannibalise the Services page (retitle from the head term to a services/process frame in `services.ts`); reinforce the homepage with a factual "why a Mallorquin studio" block + FAQ schema; internal-linking pass to the homepage with the target anchor. **Client actions (Search Console):** image sitemap already re-submitted by client; re-submit `sitemap-index.xml`; request-indexing on the 5 strongest project pages; export the 404 report (dead old image URLs → 301 to the real equivalent image); fix Google Business Profile primary category ("Jardinería" → landscape designer/architect, per 27/08 audit).
- **2026-09-01** (blog session, parallel to the image-sitemap one) — commits `8836d62`, `6ef572d`, `2c5bfde` (direct to `master`). (1) **Header language switcher fixed for blog posts** — `getAlternatePath()` can't resolve a blog-post path (EN `/slug/` vs ES/DE `/es/blog/slug/`), so every locale link fell back to that locale's homepage. `Layout.astro` now forwards its `hreflangAlternates` to `Header` as `langAlternates`; the switcher prefers those, else the path resolver (non-blog pages unchanged). (2) **`the-compounding-landscape` copy revised** in all 3 locales from a human-supplied ES rewrite — absolute claims softened throughout, opening broken out, §3 fire guidance expanded, close rebuilt with a site-reading CTA + boutique-studio sign-off; images/captions/links untouched; `description` updated. (3) **Shared Journal carousel** (`06ba2a5`) — the `/blog/` index carousel is now `src/components/site/JournalCarousel.astro` + `src/lib/carousel.js` (one copy of the swipe / keyboard-arrow / mouse-drag / scrubber controller, **no arrow buttons**). Used by both `BlogIndexPage.astro` (was ~100 lines inline markup + `.jrnl`/`.journal-*` CSS + an `initCarousel({wrap:'jrnlCar'})` call — all removed) and `BlogPost.astro`, which renders it at the foot of every post as a "Keep reading" strip of the curated lead articles (same `curatedSlugs` set, current post excluded, same-locale only). BlogIndexPage's Selected-Work (`.pf`) carousel + hero slider still use its own inline `initCarousel` — untouched. A first `.bm-*` hand-rolled version was scrapped for this. Posts without `image:` frontmatter get a stable per-slug cover via `coverBySlug` in `BlogPost.astro`. Verified in `astro preview`: index Journal + `.pf` + hero all work, post carousel 3 cards/locale, correct localized hrefs, no mobile h-overflow.
- **2026-09-01** (later) — commit `603493e` (direct to `master`). **Image sitemap rebuilt spec-valid.** Human reported images vanishing from Google since Antigravity published `sitemap-images.xml` (~Aug 20). Root cause was not the sitemap alone but (1) the mass image re-slug/rename across August + (2) ~35 `<image:loc>` entries with raw spaces/parens (`crestatx`/`llubi`/`santa-eugenia`/`sacabaneta`/`seaside-house-alcudia` renders) → Search Console flags the sitemap invalid, stalling re-discovery. Old image URLs themselves still 200 (files restored on disk in the 15/08 Antigravity session; CF serves them before the `_redirects` 301s, which sit dormant). Rewrote `scripts/generate-image-sitemap.mjs`: `toAbsoluteUrl()` does `encodeURI` + escapes `()`/`'` then XML-escapes; each file is `fs.access`-checked against `public/` and skipped-with-warning if missing; deterministic output. Regenerated `public/sitemap-images.xml` — 54 `<url>`, 227 unique images (+6 Binissalem photos that were missing, 0 dropped), all 281 image+page URLs verified 200 live, XML validated. Left alone deliberately: the `manual-image-redirects` block in `_redirects` (dormant), the big PNGs, the homepage-featured block (per-locale image dup is spec-legal). **Client action pending in Search Console**: re-submit `sitemap-images.xml`; request-indexing on the main project pages; export the 404 / "crawled not indexed" report — if old image URLs 404 there, add 301s to the *real equivalent image* (not a generic cover). Note: Drive sync pulled commit `6ef572d` (another session's blog copy revision) under this one mid-session; `src/components/site/BlogPost.astro` left modified in the working tree (not ours).
- **2026-09-01** — commit `c8bb787` (direct to `master`). Blog post byline no longer shows the publication date — `BlogPost.astro` `.blog-lead-meta` is now just `Studio · N min read` (all 3 locales; removed `pubDateLabel`/`localeTag`). `pubDate` stays in frontmatter — still drives sitemap `<lastmod>` and prev/next ordering, just not displayed (dates make evergreen articles look stale). Human asked about a live view-counter instead; deferred — a static Astro/CF-Pages site needs a KV-backed `/api/views/*` Function for that, judged not worth the infra/CLS/bot-inflation cost for now. Also **deleted** `the-regenerative-estate-...` post + its 4 mojibake SVGs (`fig1-4-*.svg`) — thin machine-drafted piece, keyword-cannibalised `the-compounding-landscape`; 301 in `_redirects` → `/the-compounding-landscape/`.
- **2026-08-31** (later) — commits `87b0c50`, `356278c` (direct to `master`). **Blog now trilingual for the 4 lead articles.** `src/content/blog/{es,de}/*.md` (lang es/de) + new routes `src/pages/{es,de}/blog/[slug].astro`. Blog rendering extracted from `[slug].astro` into shared `src/components/site/BlogPost.astro` (EN output byte-identical — verified). `Layout.astro` gained `hreflangAlternates` prop for explicit cross-locale `<link rel=alternate>` (blog posts aren't in `SITE_PAGES`, so `getAlternatePath` mislinked every blog post's `hreflang=en` to the homepage — fixed for EN-only posts too). `/blog/` "Start Here" is a carousel (reused the `initCarousel()` refactored from the Selected-Work IIFE), 4 cards incl. `the-compounding-landscape`; journal card meta localised. **Sitemap** (`astro.config.mjs` + `src/lib/blog-sitemap.mjs`): `filter` drops noindex pages (reads blog frontmatter recursively; hardcoded `/dashboard/`, `/lab/home-preview/`); real `<lastmod>` from `updatedDate ?? pubDate` (added `updatedDate` to schema); `serialize` rewrites the mis-guessed i18n `xhtml:link` hreflang for translated posts and strips it from EN-only ones. Deleted 3 empty WP-import stub posts (`design2d`, `design2d3d`, `mallorcan-sea-…`) → 301 to `/blog/` in `_redirects`; the 2 kept thin posts (`the-challenge-…`, `the-garden-is-not-…`) got explicit `noindex: true` + Instagram hashtag wall stripped.
  - To translate more posts: add `src/content/blog/{es,de}/<same-slug>.md` with `lang:` set; routes + hreflang + sitemap pick it up automatically. `curatedSlugs` in `BlogIndexPage.astro` controls the `/blog/` carousel (EN-only posts still reachable by URL + prev/next only).
- **2026-08-31** — commit `151f355` (pushed direct to `master`). New EN blog post `src/content/blog/the-compounding-landscape.md` at `/the-compounding-landscape/` — permaculture principles for high-end Mallorca gardens, written to the house pattern (modelled on `the-ultimate-guide-to-high-end-garden-design-in-mallorca`: numbered `##` sections, `*   **Term:**` bullets, `image:` frontmatter OG, hero + mid `/images/journal/` photo, internal links to `/crestatx-garden-design/`, `/sant-llorenc/`, `/hotelcabotlasvelas/`, `/regenerative-landscapes-despite-mallorcas-water-realities/`). It's a stronger companion to the thin `the-regenerative-estate-...` post (left untouched). Three portrait infographics in `public/images/diagrams/` (`cl-fig-wall-cross-section.webp`, `cl-fig-planting-layers.webp`, `cl-fig-cost-over-time.webp`) — generated by the human in ChatGPT from briefs, audited here, converted PNG→webp (≤1400px, 33–208 KB). A NotebookLM composite was rejected (said "Three-Layer" not four; "marès" garble; wide/mobile-hostile). Originals archived in `../contenido/infograficos-compounding-landscape/` with a `LEIA.txt`.
  - **Not on `/blog/`**: `BlogIndexPage.astro` shows only 3 hand-picked `curatedSlugs`; this post (like `the-regenerative-estate-...`) is reachable by direct URL + prev/next nav only. Add `the-compounding-landscape` to `curatedSlugs` if it should be featured on the index.
  - **`public/images/` layout**: blog photos → `public/images/journal/` (SEO-slug `.webp`); diagrams → `public/images/diagrams/`. There is no `public/blog/` (the human had dropped files there; moved + folder removed).
  - **Preview**: `preview_local.py` was too slow over Drive this session; used a manual C:-local mirror (`robocopy` minus node_modules/.git, `npm install` — 265 pkgs/17s, `astro dev`). `npx astro build` on the Drive still silently produces no `dist/` (known `astro.mjs` corruption).
- **2026-08-19** — commit `7e233c5`. Added the `vertical-gardens-in-mallorca` project as a Home spotlight section (mirrors the existing Campos spotlight pattern: `spotlight-grid`/`spotlight-title`/`spotlight-desc` reused, ~25 lines of new CSS only for the reversed layout + light-background text-color override), crediting the corporate collaboration with Vertical Garden Mallorca on their client OK Mobility's project. `projects.ts`: added `client: { name: "Vertical Garden Mallorca", type: "Organization", sameAs: "https://verticalgardenmallorca.com" }` (feeds the `sponsor` JSON-LD on the project page, same pattern as Hotel Cabot Las Velas) and a closing description paragraph in all 3 languages linking to verticalgardenmallorca.com. Relationship is Vertical Garden Mallorca → S'Estepa (collaborator) → OK Mobility (VGM's client, not ours) — verified this ordering against the human's correction before writing final copy; do not flip it back to "for OK Mobility" framing.
  - **Preview workflow correction**: `npm run dev` / `npx astro build` run directly against the Drive-mounted `codigo/` fail silently in this environment (matches `.agents/AGENTS.md`'s documented Google Drive file-locking issue — `astro build` exits 0 with no `/dist`, `astro dev` exits instantly with no error). The correct flow, already documented in the root `1. WEBSITES/INSTRUCOES.md` and used by other agents (Antigravity): run `python scripts/preview_local.py` from `codigo/` — it mirrors the source (minus `node_modules`/`.git`) into `%TEMP%\website-preview` on the local C: disk, `npm install`s and runs the dev server there, and cleans up the temp copy when stopped. For fast iteration after the first sync, just `cp` the one changed file into the temp mirror instead of re-running the whole script (avoids a full reinstall). Read this doc *before* attempting any local build/dev in this repo, not after hitting the failure.
- **2026-08-07** — commit `8cb2b6a` (merge of `claude/sant-llorenc-video-update-ffhitf`, `--no-ff`, branch deleted locally after merge). Replaced Sant Llorenç's EN description with a refined client-supplied translation (same "garden 2 months post-planting, preserving the ullastres" narrative as ES/DE, just a cleaner EN phrasing). Localized Jardín Mediterráneo's `location` field for SEO: added `LOCATION_OVERRIDES`/`getProjectLocation()` in `projects.ts` so it now reads "Mallorca, Balearic Islands" / "Mallorca, Islas Baleares" / "Mallorca, Balearische Inseln" per locale (previously a single untranslated "Mallorca" string, same as every project's `location` field — that field is still not localized for any other project, this was a targeted override). Updated the 7 call sites that read `project.location` (`ProjectDetail.astro`, `PortfolioPage.astro`, `HomePage.astro`, `LabHomePreview.astro`) to go through the new helper; verified build + rendered HTML on all 3 locales, confirmed other projects (e.g. Binissalem) unaffected.
  - **Branch cleanup note**: found 4 stray already-merged remote branches left over from earlier sessions (one of them, `preview`, directly against this file's own "don't keep a permanent preview branch" rule below). Tried `git push origin --delete` on those plus this session's own now-merged branch — blocked by the sandbox's egress proxy, confirmed as a structural limitation (see "Wider team context" below). Full list of the 5 pending deletions tracked in "Pending Human Action" at the top of this file, not duplicated here.
- **2026-08-07** — commit `c9712641` (parent of this doc update). Working hours 9–17 → 8–16 (Home contact block + `schema.ts` `openingHoursSpecification`, kept in sync). Services list: removed "and lighting systems" from the irrigation bullet, replaced "Proposal for outdoor materials and elements" with "Sustainability and water-efficiency assessment of the garden" (checked against the full bullet list for duplicates first). Fixed 2 broken `/gallery/` images for Jardín Mediterráneo (stale `.jpg` references left over from a site-wide JPG/PNG→WebP conversion done by the Antigravity agents). Also audited the full `docs/equipa/*` + `.agents/AGENTS.md` team documentation set — findings folded into "Wider team context" above.
