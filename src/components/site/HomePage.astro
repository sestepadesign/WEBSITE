---
import Layout from '../../layouts/Layout.astro';
import { ui } from '../../data/translations';
import { projects, getProjectLocation } from '../../data/projects';
import { testimonials, googleRating, googleReviewCount } from '../../data/testimonials';
import ProjectCard from '../ProjectCard.astro';
import { getPagePath, getProjectPath } from '../../lib/urls';

interface Props { lang: 'en' | 'es' | 'de'; }
const { lang } = Astro.props;

const t = (key: keyof typeof ui[typeof lang]) => {
  return ui[lang][key] || key;
};

// SEO meta description &mdash; leads with the exact target phrase, kept concise (~120 chars)
const descriptions = {
  en: "S'Estepa Design — Garden Design Mallorca. Boutique landscape studio for fincas, villas and terraces across the island.",
  es: "S'Estepa Design — Diseño de Jardines Mallorca. Estudio boutique de paisajismo para fincas, villas y terrazas en la isla.",
  de: "S'Estepa Design — Gartendesign Mallorca. Boutique-Landschaftsstudio für Fincas, Villen und Terrassen auf der Insel.",
};

// SEO page titles &mdash; exact keyword phrase leads, matching the pattern used by ranking competitors
const seoTitles = {
  en: "Garden Design Mallorca | S'Estepa Design",
  es: "Diseño de Jardines Mallorca | S'Estepa Design",
  de: "Gartendesign Mallorca | S'Estepa Design",
};

const pageDescription = descriptions[lang];

const whatsappTexts = {
  en: "Hello S'Estepa Design! I visited your website and would like to request a consultation.",
  es: "Hola S'Estepa Design! He visitado su web y me gustaría solicitar una consulta.",
  de: "Hallo S'Estepa Design! Ich habe Ihre Website besucht und möchte eine Beratung anfragen."
};
const whatsappUrl = `https://wa.me/34681272252?text=${encodeURIComponent(whatsappTexts[lang])}`;

// Featured projects &mdash; 2×3 grid: Llubí, Santa Eugènia, Crestatx, Binissalem, Terrace Palma, Santa Ponsa
const featuredSlugs = [
  'garden-design-llubi-mallorca',
  'terrace-garden-santa-eugenia',
  'crestatx-garden-design',
  'binissalem',
  'terrace-garden-in-palma',
  'santa-ponsa',
];
const featuredProjects = featuredSlugs
  .map((slug) => projects.find((p) => p.slug === slug))
  .filter((p): p is NonNullable<typeof p> => Boolean(p));
const camposProject = projects.find((p) => p.slug === 'finca-garden-campos-mallorca');
const santLlorencProject = projects.find((p) => p.slug === 'sant-llorenc');
const verticalGardenProject = projects.find((p) => p.slug === 'vertical-gardens-in-mallorca');

const camposSpotlightImage = camposProject?.cover ?? '';
const verticalGardenSpotlightImage = verticalGardenProject?.cover ?? '';

const spotlightCopy = {
  en: {
    tag: 'Featured Estate',
    title: 'Finca Garden Campos',
    text: 'An exceptional landscape in Campos — one of our largest estate gardens, designed and built by S\'Estepa across mathematical planting, natural meadows, and hidden garden passages.',
    cta: 'Explore the full project',
    size: '',
  },
  es: {
    tag: 'Finca Destacada',
    title: 'Finca Jardín Campos',
    text: 'Un paisaje excepcional en Campos — uno de nuestros jardines de finca más grandes, diseñado y construido por S\'Estepa con plantación matemática, praderas naturales y pasajes ocultos.',
    cta: 'Explorar el proyecto completo',
    size: '',
  },
  de: {
    tag: 'Ausgewähltes Landgut',
    title: 'Finca Garten Campos',
    text: 'Eine außergewöhnliche Landschaft in Campos — einer unserer größten Landgutgärten, entworfen und gebaut von S\'Estepa mit mathematischer Bepflanzung, natürlichen Wiesen und verborgenen Gartenpassagen.',
    cta: 'Das vollständige Projekt ansehen',
    size: '',
  },
};
const spotlight = spotlightCopy[lang];

const collabCopy = {
  en: {
    tag: 'Corporate Collaboration',
    title: 'Vertical Garden for OK Mobility',
    text: "A living wall of tropical planting, designed by Vertical Garden Mallorca in collaboration with S'Estepa, for their client OK Mobility's Mallorca offices.",
    creditLabel: 'In collaboration with',
    cta: 'Explore the full project',
  },
  es: {
    tag: 'Colaboración Corporativa',
    title: 'Jardín Vertical para OK Mobility',
    text: 'Un muro vivo de plantación tropical, diseñado por Vertical Garden Mallorca en colaboración con S\'Estepa, para las oficinas de su cliente OK Mobility en Mallorca.',
    creditLabel: 'En colaboración con',
    cta: 'Explorar el proyecto completo',
  },
  de: {
    tag: 'Unternehmenskooperation',
    title: 'Vertikaler Garten für OK Mobility',
    text: 'Eine lebendige Wand aus tropischer Bepflanzung, entworfen von Vertical Garden Mallorca in Zusammenarbeit mit S\'Estepa, für die Büros von deren Kunden OK Mobility auf Mallorca.',
    creditLabel: 'In Zusammenarbeit mit',
    cta: 'Das vollständige Projekt ansehen',
  },
};
const collab = collabCopy[lang];

const journeyData = {
  en: {
    tag: "Our Landscape Journey Design",
    title: "Your Garden Design",
    subtitle: "A highly refined, collaborative roadmap from initial meeting to a fully realized living environment.",
    steps: [
      {
        num: "01",
        title: "First Meeting",
        text: "An initial meeting to align your vision with the site’s potential. We evaluate the landscape’s unique features, orientation, and your lifestyle requirements to establish the foundation of the project.",
        hasLink: true,
        linkText: "Start the Conversation"
      },
      {
        num: "02",
        title: "Conceptual Design",
        text: "Translating our initial dialogue into a visual narrative. We develop a preliminary concept outlining the spatial layout, material palette, and key botanical features. Depending on the project’s complexity, we provide 2D scaled plans or high-fidelity 3D renders to bring the vision to life."
      },
      {
        num: "03",
        title: "Design Refinement",
        text: "A collaborative dialogue to perfect every detail. We refine the conceptual vision based on your feedback, ensuring the design aligns seamlessly with your lifestyle, functional requirements, and technical specifications."
      },
      {
        num: "04",
        title: "Final Design",
        text: "Once the design concept is finalized, we develop the comprehensive technical plans and specifications required for execution. This stage defines every constructive detail, from the precise selection of hardscape materials to the complete botanical palette, ensuring a rigorous roadmap for the build."
      },
      {
        num: "05",
        title: "Installation",
        text: "Our dedicated local team brings the project to life, managing the entire construction and planting process on-site. We oversee every technical specification to ensure the garden is delivered to our exacting standards, providing a seamless transition from the final plans to a living landscape."
      }
    ]
  },
  es: {
    tag: "Nuestra Trayectoria de Paisajismo",
    title: "Su Diseño de Jardín",
    subtitle: "Una hoja de ruta altamente refinada y colaborativa, desde el primer encuentro hasta un entorno vivo completamente realizado.",
    steps: [
      {
        num: "01",
        title: "Primer Encuentro",
        text: "Una reunión inicial para alinear su visión con el potencial del terreno. Evaluamos las características únicas del paisaje, la orientación y sus requisitos de estilo de vida para establecer las bases del proyecto.",
        hasLink: true,
        linkText: "Iniciar una Conversación"
      },
      {
        num: "02",
        title: "Diseño Conceptual",
        text: "Traducimos nuestro diálogo inicial en una narrativa visual. Desarrollamos un concepto preliminar que define la distribución espacial, la paleta de materiales y los elementos botánicos clave. Según la complejidad del proyecto, proporcionamos planos a escala 2D o renders 3D de alta fidelidad."
      },
      {
        num: "03",
        title: "Refinamiento del Diseño",
        text: "Un diálogo colaborativo para perfeccionar cada detalle. Refinamos la visión conceptual basándonos en sus comentarios, asegurando que el diseño se adapte a su estilo de vida, necesidades funcionales y especificaciones técnicas."
      },
      {
        num: "04",
        title: "Diseño Final",
        text: "Una vez finalizado el concepto de diseño, desarrollamos los planos técnicos completos y las especificaciones para la ejecución. Esta etapa define cada detalle constructivo, desde los materiales hasta la paleta botánica completa, asegurando una hoja de ruta rigurosa para la obra."
      },
      {
        num: "05",
        title: "Instalación",
        text: "Nuestro equipo local dedicado da vida al proyecto, gestionando todo el proceso de construcción y plantación in situ. Supervisamos cada especificación técnica para garantizar que el jardín se entregue según nuestros estándares exigentes, asegurando una transición fluida."
      }
    ]
  },
  de: {
    tag: "Unser Landschaftsweg Design",
    title: "Ihre Gartengestaltung",
    subtitle: "Ein hochgradig verfeinerter, partnerschaftlicher Fahrplan vom ersten Treffen bis zu einer vollendeten lebendigen Umgebung.",
    steps: [
      {
        num: "01",
        title: "Erstes Treffen",
        text: "Ein erstes Gespräch, um Ihre Vision mit dem Potenzial des Standorts abzustimmen. Wir bewerten die einzigartigen Merkmale der Landschaft, die Ausrichtung und Ihre Lebensstil-Anforderungen, um die Grundlage des Projekts zu schaffen.",
        hasLink: true,
        linkText: "Ein Gespräch Beginnen"
      },
      {
        num: "02",
        title: "Konzeptentwurf",
        text: "Übersetzung unseres ersten Dialogs in eine visuelle Erzählung. Wir entwickeln ein vorläufiges Konzept, das die räumliche Aufteilung, die Materialpalette und die wichtigsten botanischen Merkmale umreißt. Je nach Komplexität des Projekts erstellen wir 2D-Pläne oder hochauflösende 3D-Visualisierungen."
      },
      {
        num: "03",
        title: "Detailabstimmung",
        text: "Ein partnerschaftlicher Dialog, um jedes Detail zu perfektionieren. Wir verfeinern den Konzeptentwurf basierend auf Ihrem Feedback und stellen sicher, dass das Design nahtlos zu Ihrem Lebensstil, Ihren funktionalen Anforderungen und den technischen Spezifikationen passt."
      },
      {
        num: "04",
        title: "Ausführungsplanung",
        text: "Sobald das Designkonzept feststeht, erstellen wir die detaillierten technischen Pläne und Spezifikationen, die für die Ausführung erforderlich sind. Diese Phase definiert jedes bauliche Detail, von der präzisen Auswahl der Materialien bis hin zur vollständigen botanischen Palette."
      },
      {
        num: "05",
        title: "Realisierung",
        text: "Unser engagiertes lokales Team erweckt das Projekt zum Leben und leitet den gesamten Bau- und Pflanzprozess vor Ort. Wir überwachen jede technische Spezifikation, um sicherzustellen, dass der Garten nach unseren anspruchsvollen Standards übergeben wird."
      }
    ]
  }
};

const servicesData = {
  en: {
    tag: "Our Expertise",
    title: "Services",
    subtitle: "We provide a comprehensive approach to landscaping in Mallorca, focusing on the creation and long-term maintenance of living ecosystems for villas and holiday residences. Our team oversees the entire process—from design and construction to technical irrigation and planting. We ensure a seamless experience, managing every detail in English, Spanish, and German.",
    items: [
      {
        title: "Villas & Holiday Homes",
        text: "Exclusive tailor-made botanical environments designed to enrich refined residences, holiday homes, and fincas across Mallorca.",
        linkText: "View Projects",
        linkUrl: "portfolio"
      },
      {
        title: "Landscape Design",
        text: "High-fidelity conceptual drawings, technical layouts, and virtual 3D renders that breathe spatial order and structure into your vision.",
        linkText: "Meet Our Architect",
        linkUrl: "about"
      },
      {
        title: "Full Garden Construction",
        text: "Rigorous site development, structural earthworks, and planting executed by our dedicated team of local professionals.",
        linkText: "Meet Site Manager",
        linkUrl: "about"
      },
      {
        title: "Ecological Care",
        text: "Xeriscape planting schemes, water resource planning, and intelligent drip irrigation systems designed for ultimate sustainability.",
        linkText: "Request Consultation",
        linkUrl: "contact"
      }
    ]
  },
  es: {
    tag: "Nuestra Experiencia",
    title: "Servicios",
    subtitle: "Ofrecemos un enfoque integral del paisajismo en Mallorca, centrándonos en la creación y el mantenimiento a largo plazo de ecosistemas vivos para villas y residencias vacacionales. Nuestro equipo supervisa todo el proceso, desde el diseño y la construcción hasta el riego técnico y la plantación. Garantizamos una experiencia sin fisuras, gestionando cada detalle en inglés, español y alemán.",
    items: [
      {
        title: "Villas y Fincas",
        text: "Entornos botánicos exclusivos y a medida, diseñados para enriquecer distinguidas residencias, villas de vacaciones y fincas en Mallorca.",
        linkText: "Ver Proyectos",
        linkUrl: "portfolio"
      },
      {
        title: "Diseño de Paisaje",
        text: "Planos conceptuales de alta fidelidad, distribuciones técnicas y renders 3D virtuales que aportan orden y estructura espacial a su visión.",
        linkText: "Conozca a la Arquitecta",
        linkUrl: "about"
      },
      {
        title: "Construcción y Obra",
        text: "Desarrollo riguroso de la obra, movimientos de tierra estructurales y plantación ejecutada por nuestro equipo local de profesionales.",
        linkText: "Conozca al Jefe de Obra",
        linkUrl: "about"
      },
      {
        title: "Cuidado Ecológico",
        text: "Diseños de xerojardinería, planificación de recursos hídricos y sistemas inteligentes de riego por goteo para la máxima sostenibilidad.",
        linkText: "Solicitar Consulta",
        linkUrl: "contact"
      }
    ]
  },
  de: {
    tag: "Unsere Expertise",
    title: "Dienstleistungen",
    subtitle: "Wir bieten einen umfassenden Ansatz für die Landschaftsgestaltung auf Mallorca, mit Schwerpunkt auf der Schaffung und langfristigen Pflege lebendiger Ökosysteme für Villen und Feriendomizile. Unser Team überwacht den gesamten Prozess – vom Entwurf und Bau bis hin zur technischen Bewässerung und Bepflanzung. Wir garantieren einen reibungslosen Ablauf und regeln jedes Detail auf Englisch, Spanisch und Deutsch.",
    items: [
      {
        title: "Villen & Feriendomizile",
        text: "Exklusive, maßgeschneiderte botanische Umgebungen, die anspruchsvolle Residenzen, Feriendomizile und Fincas auf ganz Mallorca bereichern.",
        linkText: "Projekte Ansehen",
        linkUrl: "portfolio"
      },
      {
        title: "Landschaftsarchitektur",
        text: "Präzise Konzeptzeichnungen, technische Layouts und virtuelle 3D-Renderings, die Ihrer Vision räumliche Ordnung und Struktur verleihen.",
        linkText: "Treffen Sie die Architektin",
        linkUrl: "about"
      },
      {
        title: "Kompletter Gartenbau",
        text: "Strikte Baustellenentwicklung, strukturelle Erdarbeiten und Pflanzung, ausgeführt von unserem engagierten Team lokaler Fachleute.",
        linkText: "Treffen Sie den Bauleiter",
        linkUrl: "about"
      },
      {
        title: "Ökologische Pflege",
        text: "Xeriscape-Bepflanzungspläne, Wasserressourcenplanung und Tröpfchenbewässerungssysteme für höchste Nachhaltigkeit.",
        linkText: "Beratung Anfragen",
        linkUrl: "contact"
      }
    ]
  }
};

const currentServices = servicesData[lang];
const currentJourney = journeyData[lang];

// Client testimonials — sourced from src/data/testimonials.ts, the single
// source of truth also consumed by schema.ts, so the visible carousel and
// the Review/AggregateRating JSON-LD never drift apart (Google spam policy:
// structured data must reflect visible page content). Genuine Google
// reviews; attribution uses first name + surname initial only, per client
// privacy preference for this ultra-exclusive clientele.
const testimonialsCopy = {
  en: { tag: 'Client Voices', title: 'What Our Clients Say', subtitle: `${googleRating} on Google · ${googleReviewCount} reviews`, readMore: 'Read full review on Google' },
  es: { tag: 'Voces de Clientes', title: 'Lo Que Dicen Nuestros Clientes', subtitle: `${googleRating} en Google · ${googleReviewCount} reseñas`, readMore: 'Leer reseña completa en Google' },
  de: { tag: 'Stimmen Unserer Kunden', title: 'Was Unsere Kunden Sagen', subtitle: `${googleRating} auf Google · ${googleReviewCount} Bewertungen`, readMore: 'Vollständige Bewertung auf Google lesen' },
};
const currentTestimonials = testimonialsCopy[lang];
---

<Layout title={seoTitles[lang]} description={pageDescription} lang={lang}>
  <!-- Hero Section with Background Video -->
  <section class="hero" id="hero-section">
    <div class="video-container">
      <img src="/videos/sant_llorenc_poster.webp" class="hero-poster-img" fetchpriority="high" alt="S'Estepa Design" width="1920" height="1080" />
      <video autoplay loop muted playsinline class="hero-video" aria-hidden="true" id="heroVideo" data-src="/videos/sant_llorenc_hero.mp4">
      </video>
      <div class="hero-overlay"></div>
    </div>
    <script is:inline>
      document.addEventListener('DOMContentLoaded', function() {
        if (window.matchMedia('(min-width: 768px)').matches) {
          var video = document.getElementById('heroVideo');
          var source = document.createElement('source');
          source.src = video.getAttribute('data-src');
          source.type = 'video/mp4';
          video.appendChild(source);
        }
      });
    </script>
    
    <div class="hero-content container">
      <h1 class="hero-title">
        <span class="hero-eyebrow">{t('hero.subtitle')}</span>
        {lang === 'en' && <>Sustainable Mediterranean Gardens</>}
        {lang === 'es' && <>Jardines Mediterráneos Sostenibles</>}
        {lang === 'de' && <>Nachhaltige Mediterrane Gärten</>}
      </h1>
      <a href={getPagePath('portfolio', lang)} class="hero-btn">
        {t('hero.button')} <span class="arrow">&rarr;</span>
      </a>
    </div>
    
    <div class="scroll-indicator">
      <span class="scroll-text">Scroll</span>
      <span class="scroll-line"></span>
    </div>
  </section>

  <!-- Poetry & Philosophy Section -->
  <section class="philosophy section-padding">
    <div class="container philosophy-container">
      <span class="section-tag">{t('about.title')}</span>
      <h2 class="philosophy-title">{t('about.subtitle')}</h2>
      <div class="philosophy-grid">
        <div class="philosophy-text-col">
          {lang === 'en' && (
            <>
              <p>S'Estepa Design was founded in Mallorca in 1999. Pedro Campaner started designing gardens on the island at eighteen; three decades on, the studio is a team of local professionals who have spent their working lives reading this particular terrain.</p>
              <p>We design for Mallorca as it actually is — limestone and marès soil, the tramuntana wind, long dry summers and real water limits — working with endemic garriga species, dry-stone walls and the ancient wild olives left standing.</p>
              <p>Every garden is managed from the first site visit to the finished planting, in English, Spanish and German. <a href={getPagePath('about', lang)}>More about the studio</a>.</p>
            </>
          )}
          {lang === 'es' && (
            <>
              <p>S'Estepa Design nació en Mallorca en 1999. Pedro Campaner empezó a diseñar jardines en la isla a los dieciocho años; tres décadas después, el estudio es un equipo de profesionales locales que han dedicado su vida laboral a leer este terreno concreto.</p>
              <p>Diseñamos para la Mallorca real: suelo calcáreo y marès, la tramontana, veranos largos y secos y límites de agua reales, trabajando con especies endémicas de la garriga, muros de piedra seca y los viejos acebuches que dejamos en pie.</p>
              <p>Cada jardín se gestiona desde la primera visita hasta la última plantación, en español, inglés y alemán. <a href={getPagePath('about', lang)}>Más sobre el estudio</a>.</p>
            </>
          )}
          {lang === 'de' && (
            <>
              <p>S'Estepa Design wurde 1999 auf Mallorca gegründet. Pedro Campaner begann mit achtzehn Jahren, Gärten auf der Insel zu gestalten; drei Jahrzehnte später ist das Studio ein Team lokaler Fachleute, die ihr Berufsleben dem Lesen genau dieses Geländes gewidmet haben.</p>
              <p>Wir gestalten für Mallorca, wie es wirklich ist — Kalk- und Marès-Boden, der Tramuntana-Wind, lange trockene Sommer und echte Wassergrenzen — mit endemischen Garriga-Arten, Trockenmauern und den alten wilden Olivenbäumen, die stehen bleiben.</p>
              <p>Jeder Garten wird von der ersten Besichtigung bis zur fertigen Bepflanzung begleitet, auf Deutsch, Spanisch und Englisch. <a href={getPagePath('about', lang)}>Mehr über das Studio</a>.</p>
            </>
          )}
        </div>
        <div class="philosophy-stat-col">
          <div class="stat-box">
            <span class="stat-number">30</span>
            <span class="stat-label">
              {lang === 'en' && "Years of Expertise"}
              {lang === 'es' && "Años de Experiencia"}
              {lang === 'de' && "Jahre Erfahrung"}
            </span>
          </div>
          <div class="stat-box">
            <span class="stat-number">100%</span>
            <span class="stat-label">
              {lang === 'en' && "Mediterranean Soul"}
              {lang === 'es' && "Alma Mediterránea"}
              {lang === 'de' && "Mediterrane Seele"}
            </span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Featured Portfolio Grid Section -->
  <section class="featured section-padding">
    <div class="container">
      <div class="featured-header">
        <div>
          <span class="section-tag">{t('portfolio.title')}</span>
          <h2 class="featured-title">
            {lang === 'en' && "Landscape Garden Design Projects"}
            {lang === 'es' && "Proyectos de Diseño de Jardines y Paisajismo"}
            {lang === 'de' && "Landschafts- und Gartengestaltungsprojekte"}
          </h2>
        </div>
        <a href={getPagePath('portfolio', lang)} class="text-link">
          {lang === 'en' && "View all gardens"}
          {lang === 'es' && "Ver todos los jardines"}
          {lang === 'de' && "Alle Gärten ansehen"} &rarr;
        </a>
      </div>

      <div class="projects-grid featured-projects-grid">
        {santLlorencProject && (
          <div class="featured-item featured-item-large">
            <ProjectCard
              slug={santLlorencProject.slug}
              title={santLlorencProject[lang].title}
              category={santLlorencProject[lang].category}
              location={getProjectLocation(santLlorencProject, lang)}
              cover={santLlorencProject.cover}
              coverSrcset="/portfolio/sant-llorenc/images/garden-design-mallorca-sestepa-landscape-design-sant-llorenc-18-card.webp"
              lang={lang}
              large={true}
              size={santLlorencProject.size}
              extraImages={[
                "/portfolio/sant-llorenc/images/garden-design-mallorca-sestepa-landscape-design-sant-llorenc-15.webp",
                "/portfolio/sant-llorenc/images/garden-design-mallorca-sestepa-landscape-design-sant-llorenc-01.webp"
              ]}
              extraImagesSrcset={[
                "/portfolio/sant-llorenc/images/garden-design-mallorca-sestepa-landscape-design-sant-llorenc-15-card.webp",
                "/portfolio/sant-llorenc/images/garden-design-mallorca-sestepa-landscape-design-sant-llorenc-01-card.webp"
              ]}
            />
          </div>
        )}
        {featuredProjects.map((project) => {
          let coverSrcset = undefined;
          if (project.slug === 'crestatx-garden-design') {
            coverSrcset = "/portfolio/crestatx-garden-design/garden-design-mallorca-crestatx-garden-design-sestepa-card.webp";
          } else if (project.slug === 'garden-design-llubi-mallorca') {
            coverSrcset = "/portfolio/garden-design-llubi-mallorca/images/landscape-architecture-mallorca-llubi-sestepa-design%20(1)-card.webp";
          } else if (project.slug === 'terrace-garden-in-palma') {
            coverSrcset = "/portfolio/terrace-garden-in-palma/garden-design-mallorca-terrace-garden-in-palma-sestepa-card.webp";
          } else if (project.slug === 'santa-ponsa') {
            coverSrcset = "/portfolio/santa-ponsa/images/GARDEN-DESIGN-MALLORCA-SANTA-PONSA-SESTEPA-1-card.webp";
          } else if (project.slug === 'binissalem') {
            coverSrcset = "/portfolio/binissalem/garden-design-mallorca-binissalem-sestepa-card.webp";
          } else if (project.slug === 'terrace-garden-santa-eugenia') {
            coverSrcset = "/portfolio/terrace-garden-santa-eugenia/images/garden-design-mallorca-santa-eugenia-sestepa-card.webp";
          }

          return (
            <div class="featured-item" key={project.slug}>
              <ProjectCard
                slug={project.slug}
                title={project[lang].title}
                category={project[lang].category}
                location={getProjectLocation(project, lang)}
                cover={project.cover}
                coverSrcset={coverSrcset}
                lang={lang}
                size={project.size}
              />
            </div>
          );
        })}
      </div>
    </div>
  </section>

  {camposProject && (
    <section class="campos-spotlight section-padding">
      <div class="container spotlight-grid">
        <a href={getProjectPath('finca-garden-campos-mallorca', lang)} class="spotlight-image-link">
          <picture>
            <source media="(max-width: 768px)" srcset="/portfolio/finca-garden-campos-mallorca/garden-design-mallorca-finca-garden-campos-mallorca-sestepa-card.webp" />
            <img
              src={camposSpotlightImage}
              alt={camposProject[lang].title}
              class="spotlight-image"
              loading="lazy"
              decoding="async"
            />
          </picture>
        </a>
        <div class="spotlight-text">
          <span class="section-tag">{spotlight.tag}</span>
          <h2 class="spotlight-title">{spotlight.title}</h2>
          {spotlight.size && <p class="spotlight-size">{spotlight.size}</p>}
          <p class="spotlight-desc">{spotlight.text}</p>
          <a href={getProjectPath('finca-garden-campos-mallorca', lang)} class="text-link">
            {spotlight.cta} &rarr;
          </a>
        </div>
      </div>
    </section>
  )}

  {verticalGardenProject && (
    <section class="collab-spotlight section-padding">
      <div class="container spotlight-grid collab-grid">
        <div class="spotlight-text">
          <span class="section-tag">{collab.tag}</span>
          <h2 class="spotlight-title">{collab.title}</h2>
          <p class="spotlight-desc">{collab.text}</p>
          <p class="collab-credit">
            {collab.creditLabel} <a href="https://verticalgardenmallorca.com" target="_blank" rel="noopener noreferrer" class="collab-link">Vertical Garden Mallorca</a>
          </p>
          <a href={getProjectPath('vertical-gardens-in-mallorca', lang)} class="text-link">
            {collab.cta} &rarr;
          </a>
        </div>
        <a href={getProjectPath('vertical-gardens-in-mallorca', lang)} class="spotlight-image-link">
          <picture>
            <source media="(max-width: 768px)" srcset="/portfolio/vertical-gardens-in-mallorca/garden-design-mallorca-vertical-gardens-in-mallorca-sestepa-card.webp" />
            <img
              src={verticalGardenSpotlightImage}
              alt={verticalGardenProject[lang].title}
              class="spotlight-image"
              loading="lazy"
              decoding="async"
            />
          </picture>
        </a>
      </div>
    </section>
  )}

  <!-- Services Section -->
  <section class="services-section section-padding">
    <div class="container">
      <span class="section-tag center">{currentServices.tag}</span>
      <h2 class="section-heading center">{currentServices.title}</h2>
      <p class="section-desc center">{currentServices.subtitle}</p>
      
      <div class="category-grid">
        {currentServices.items.map((item, index) => (
          <div class="category-card">
            <div class="category-icon-wrapper">
              {index === 0 && (
                <svg class="service-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M4 21V9l8-5 8 5v12H4z" />
                  <path d="M9 21v-8h6v8" />
                  <path d="M7 9h10" />
                </svg>
              )}
              {index === 1 && (
                <svg class="service-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M12 2v3" />
                  <path d="M12 5L7 20" />
                  <path d="M12 5l5 15" />
                  <path d="M9 14h6" />
                  <circle cx="12" cy="5" r="1.5" />
                </svg>
              )}
              {index === 2 && (
                <svg class="service-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M20 20H4v-4h16v4z" />
                  <path d="M14 16H4v-4h10v4z" />
                  <path d="M20 16h-6v-4h6v4z" />
                  <path d="M10 12H4V8h6v4z" />
                  <path d="M20 12H10V8h10v4z" />
                </svg>
              )}
              {index === 3 && (
                <svg class="service-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M12 22a7 7 0 0 0 7-7c0-4.3-7-13-7-13S5 10.7 5 15a7 7 0 0 0 7 7z" />
                  <path d="M12 9v9" />
                  <path d="M12 12s2 1.5 2 3" />
                  <path d="M12 14s-2 1.5-2 3" />
                </svg>
              )}
            </div>
            <h3 class="category-title">{item.title}</h3>
            <p class="category-text">{item.text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Landscape Journey Design Section -->
  <section class="workflow-section">
    <div class="container workflow-container">
      <span class="section-tag center">{currentJourney.tag}</span>
      <h2 class="section-heading center">{currentJourney.title}</h2>
      <p class="section-desc center">{currentJourney.subtitle}</p>

      <div class="workflow-timeline">
        {currentJourney.steps.map((step) => (
          <div class="workflow-step">
            <div class="workflow-num">{step.num}.</div>
            <div class="workflow-content">
              <h3>{step.title}</h3>
              <p class="paragraph">
                {step.text}
                {step.hasLink && (
                  <>
                    {' '}
                    <a href={getPagePath('contact', lang)} class="workflow-link">
                      {step.linkText}
                    </a>
                  </>
                )}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Testimonials Section -->
  <section class="testimonials-section section-padding">
    <div class="container">
      <span class="section-tag center">{currentTestimonials.tag}</span>
      <h2 class="section-heading center">{currentTestimonials.title}</h2>
      <p class="testimonials-subtitle center">{currentTestimonials.subtitle}</p>

      <div class="testimonials-carousel">
        <button class="carousel-nav carousel-prev" type="button" aria-label="Previous">
          <span class="arrow">&larr;</span>
        </button>

        <div class="testimonials-track" id="testimonialsTrack">
          {testimonials.map((item) => (
            <blockquote class="testimonial-card">
              <div class="testimonial-stars" aria-hidden="true">{'★'.repeat(item.rating)}{'☆'.repeat(5 - item.rating)}</div>
              <p class="testimonial-quote">&ldquo;{item.quote[lang]}&rdquo;</p>
              <footer class="testimonial-footer">
                <span class="testimonial-author">— {item.author}</span>
                {item.long && (
                  <a href={item.sourceUrl} target="_blank" rel="noopener noreferrer" class="testimonial-link">
                    {currentTestimonials.readMore} <span class="arrow">&rarr;</span>
                  </a>
                )}
              </footer>
            </blockquote>
          ))}
        </div>

        <button class="carousel-nav carousel-next" type="button" aria-label="Next">
          <span class="arrow">&rarr;</span>
        </button>
      </div>
    </div>
  </section>

  <!-- Contact Form Section -->
  <section class="contact-section section-padding">
    <div class="container">
      <header class="contact-header">
        <span class="section-tag center">{t('nav.contact')}</span>
        <h2 class="section-heading center">{t('contact.title')}</h2>
        <p class="section-desc center">{t('contact.subtitle')}</p>
      </header>

      <div class="contact-grid">
        <!-- Form Block -->
        <div class="contact-form-container">
          <form class="contact-form" action="#" method="POST" id="inquiryForm">
            <div class="form-group">
              <label for="name">{t('contact.form.name')}</label>
              <input type="text" id="name" name="name" required placeholder=" " />
            </div>

            <div class="form-group">
              <label for="email">{t('contact.form.email')}</label>
              <input type="email" id="email" name="email" required placeholder=" " />
            </div>

            <div class="form-group">
              <label for="phone">{t('contact.form.phone')}</label>
              <input type="tel" id="phone" name="phone" placeholder=" " />
            </div>

            <div class="form-group">
              <label for="subject">{t('contact.form.location')}</label>
              <input type="text" id="subject" name="subject" required placeholder={t('contact.form.location_placeholder')} />
            </div>

            <div class="form-group">
              <label for="message">{t('contact.form.message')}</label>
              <textarea id="message" name="message" rows="6" required placeholder=" "></textarea>
            </div>

            <button type="submit" class="submit-btn">
              {t('contact.form.submit')} &rarr;
            </button>
            
            <div class="form-status" id="formStatus" aria-live="polite"></div>
          </form>
        </div>

        <!-- Info Block -->
        <div class="contact-info-panel">
          <div class="info-block">
            <h3 class="info-label">
              {lang === 'en' && "Studio Address"}
              {lang === 'es' && "Dirección del Estudio"}
              {lang === 'de' && "Studio-Adresse"}
            </h3>
            <p class="info-text">
              Mallorca, Balearic Islands<br />
              Spain
            </p>
          </div>

          <div class="info-block">
            <h3 class="info-label">
              {lang === 'en' && "Direct Contact"}
              {lang === 'es' && "Contacto Directo"}
              {lang === 'de' && "Direkter Kontakt"}
            </h3>
            <p class="info-text">
              Email: <a href="mailto:sofia@sestepa.com" class="info-link">sofia@sestepa.com</a>
            </p>
            <p class="info-text">
              WhatsApp: <a href={whatsappUrl} class="info-link" target="_blank" rel="noopener noreferrer">+34 681 272 252</a>
            </p>
          </div>

          <div class="info-block">
            <h3 class="info-label">
              {lang === 'en' && "Working Hours"}
              {lang === 'es' && "Horario de Atención"}
              {lang === 'de' && "Arbeitszeiten"}
            </h3>
            <p class="info-text">
              {lang === 'en' && "Monday to Friday: 8:00 - 16:00"}
              {lang === 'es' && "Lunes a Viernes: 8:00 - 16:00"}
              {lang === 'de' && "Montag bis Freitag: 8:00 - 16:00"}
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <script>
    // Google Apps Script Web App Integration for form submission
    const form = document.getElementById('inquiryForm') as HTMLFormElement;
    const statusDiv = document.getElementById('formStatus');

    if (form && statusDiv) {
      form.addEventListener('submit', (e) => {
        e.preventDefault();
        
        const submitBtn = form.querySelector('.submit-btn') as HTMLButtonElement;
        const originalText = submitBtn.innerHTML;
        submitBtn.textContent = "Sending...";
        submitBtn.disabled = true;
        
        const formData = new FormData(form);
        const data: Record<string, string> = {};
        formData.forEach((val, key) => { data[key] = String(val); });
        
        if (data.phone) {
          data.message = `[Phone: ${data.phone}]\n\n${data.message || ''}`;
        }
        
        data.token = "sestepa_secure_2026";
        data.lang = document.documentElement.lang || "en";
        
        const actionUrl = "https://script.google.com/macros/s/AKfycbzR8Kdmb5xfZv1___-qDOmeWgYuSOfwWlqQb00_6x37EACe4xKpgnlU5ZHRJuCPoZIszQ/exec";
        const queryString = new URLSearchParams(data).toString();
        
        fetch(`${actionUrl}?${queryString}`, {
          method: "GET",
          mode: "no-cors"
        })
        .then(() => {
          statusDiv.textContent = "Thank you. Your message has been sent successfully.";
          statusDiv.classList.add('success');
          // Dispara a conversão no Google Ads (mesma tag oficial do contactPage)
          if (typeof window !== 'undefined' && typeof window.gtag === 'function') {
            window.gtag('event', 'conversion', {'send_to': 'AW-795871100/dv-kCJrn0YoaEPyOwPsC'});
          }
          form.reset();
        })
        .catch(err => {
          console.error("Transmission error:", err);
          // Fallback success feedback as it may fail CORS but succeeds in transmitting
          statusDiv.textContent = "Thank you. Your message has been sent successfully.";
          statusDiv.classList.add('success');
          // Dispara a conversão também no fallback
          if (typeof window !== 'undefined' && typeof window.gtag === 'function') {
            window.gtag('event', 'conversion', {'send_to': 'AW-795871100/dv-kCJrn0YoaEPyOwPsC'});
          }
          form.reset();
        })
        .finally(() => {
          submitBtn.innerHTML = originalText;
          submitBtn.disabled = false;
        });
      });
    }
  </script>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const track = document.getElementById('testimonialsTrack');
      const prevBtn = document.querySelector('.carousel-prev');
      const nextBtn = document.querySelector('.carousel-next');
      if (!track || !prevBtn || !nextBtn) return;

      const scrollByCard = (direction: number) => {
        const card = track.querySelector('.testimonial-card');
        const cardWidth = card ? card.getBoundingClientRect().width : track.clientWidth;
        const gap = 40; // matches .testimonials-track gap (2.5rem)
        track.scrollBy({ left: direction * (cardWidth + gap), behavior: 'smooth' });
      };

      prevBtn.addEventListener('click', () => scrollByCard(-1));
      nextBtn.addEventListener('click', () => scrollByCard(1));
    });
  </script>
</Layout>

<style>
  /* Hero Section */
  .hero {
    position: relative;
    height: calc(100vh - 4px);
    width: 100%;
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  .video-container {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .hero-poster-img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 1;
  }

  .hero-video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 2;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      to bottom,
      rgba(31, 33, 31, 0.55) 0%,
      rgba(31, 33, 31, 0.2) 60%,
      rgba(250, 249, 246, 1) 100%
    );
    z-index: 3;
  }

  .hero-content {
    position: relative;
    z-index: 3;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1.5rem;
    padding-top: 5rem;
  }

  .hero-title {
    font-size: clamp(3rem, 8vw, 6rem);
    font-weight: 300;
    color: var(--color-white);
    line-height: 1.1;
    max-width: 900px;
  }

  .hero-eyebrow {
    display: block;
    font-family: var(--font-sans);
    font-size: 0.9rem;
    font-weight: 400;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--color-sand-base);
    margin-bottom: 1.5rem;
  }

  .hero-title em {
    font-family: var(--font-serif);
    font-style: italic;
    font-weight: 300;
  }

  .hero-btn {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-white);
    border: 1px solid rgba(255, 255, 255, 0.4);
    padding: 1.25rem 2.5rem;
    background: rgba(31, 33, 31, 0.15);
    backdrop-filter: blur(5px);
    transition: var(--transition-medium);
    margin-top: 1rem;
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .hero-btn:hover {
    background: var(--color-white);
    color: var(--color-charcoal);
    border-color: var(--color-white);
  }

  .hero-btn .arrow {
    transition: transform 0.3s ease;
  }

  .hero-btn:hover .arrow {
    transform: translateX(6px);
  }

  .scroll-indicator {
    position: absolute;
    bottom: 3rem;
    right: 3rem;
    z-index: 3;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .scroll-text {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.25em;
    color: var(--color-charcoal);
    writing-mode: vertical-lr;
    opacity: 0.6;
  }

  .scroll-line {
    width: 1px;
    height: 60px;
    background-color: var(--color-charcoal);
    opacity: 0.3;
    position: relative;
    overflow: hidden;
  }

  .scroll-line::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 30%;
    background-color: var(--color-olive);
    animation: scrollDown 2s infinite ease-in-out;
  }

  /* Philosophy Section */
  .philosophy {
    background-color: var(--color-sand-light);
    border-bottom: 1px solid var(--color-sand-dark);
    padding: 6rem 0;
  }

  .philosophy-container {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  .section-tag {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-olive-dark);
    display: block;
    margin-bottom: 1rem;
  }

  .philosophy-title {
    font-family: var(--font-serif);
    font-size: clamp(2.25rem, 3.8vw, 3.25rem);
    line-height: 1.25;
    max-width: 920px;
    font-weight: 300;
    letter-spacing: -0.01em;
    white-space: pre-line;
  }

  .philosophy-grid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 6rem;
    align-items: start;
    margin-top: 1.25rem;
  }

  .philosophy-text-col {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .philosophy-text-col p {
    font-size: 1.15rem;
    color: var(--color-charcoal-light);
    line-height: 1.8;
  }

  .philosophy-text-col a {
    color: var(--color-olive);
    border-bottom: 1px solid currentColor;
    padding-bottom: 1px;
    transition: var(--transition-fast);
  }

  .philosophy-text-col a:hover {
    color: var(--color-olive-dark);
  }

  .philosophy-seo-lead {
    font-family: var(--font-serif);
    font-size: 1.25rem;
    line-height: 1.65;
    color: var(--color-charcoal);
    font-weight: 400;
    margin-bottom: 0.25rem;
  }

  .philosophy-stat-col {
    display: flex;
    flex-direction: column;
    gap: 3rem;
    border-left: 1px solid var(--color-sand-dark);
    padding-left: 3rem;
  }

  .stat-box {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .stat-number {
    font-family: var(--font-serif);
    font-size: 4rem;
    font-weight: 300;
    line-height: 1;
    color: var(--color-olive-dark);
  }

  .stat-label {
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-limestone);
  }

  /* Campos spotlight */
  .campos-spotlight {
    background-color: var(--color-charcoal);
    color: var(--color-white);
  }

  .spotlight-grid {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .spotlight-image-link {
    display: block;
    overflow: hidden;
  }

  .spotlight-image {
    width: 100%;
    aspect-ratio: 16 / 10;
    object-fit: cover;
    transition: transform 1.4s cubic-bezier(0.25, 1, 0.5, 1);
  }

  .spotlight-image-link:hover .spotlight-image {
    transform: scale(1.03);
  }

  .spotlight-title {
    font-family: var(--font-serif);
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 300;
    margin: 0.75rem 0;
    color: var(--color-white);
  }

  .spotlight-size {
    font-size: 0.85rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.65);
    margin-bottom: 1.25rem;
  }

  .spotlight-desc {
    line-height: 1.8;
    color: rgba(255, 255, 255, 0.82);
    margin-bottom: 2rem;
    max-width: 520px;
  }

  .campos-spotlight .section-tag {
    color: rgba(255, 255, 255, 0.7);
  }

  .campos-spotlight .text-link {
    color: var(--color-white);
    border-bottom-color: rgba(255, 255, 255, 0.35);
  }

  /* Collaboration spotlight (Vertical Garden × OK Mobility) */
  .collab-spotlight {
    background-color: var(--color-sand-light);
    border-top: 1px solid var(--color-sand-dark);
    border-bottom: 1px solid var(--color-sand-dark);
  }

  .collab-grid {
    grid-template-columns: 1fr 1.2fr;
  }

  .collab-spotlight .spotlight-title {
    color: var(--color-charcoal);
  }

  .collab-spotlight .spotlight-desc {
    color: var(--color-charcoal-light);
  }

  .collab-credit {
    font-size: 0.85rem;
    color: var(--color-charcoal-light);
    margin: -0.75rem 0 1.75rem;
  }

  .collab-link {
    color: var(--color-olive-dark);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .collab-link:hover {
    color: var(--color-olive);
  }

  /* Featured Section */
  .featured {
    background-color: var(--color-white);
  }

  .featured-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: 4rem;
  }

  .featured-title {
    font-size: clamp(2rem, 3.5vw, 3rem);
    font-weight: 300;
  }

  .text-link {
    font-size: 0.9rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-olive);
    border-bottom: 1px solid var(--color-sand-dark);
    padding-bottom: 0.5rem;
    transition: var(--transition-fast);
  }

  .text-link:hover {
    color: var(--color-olive-dark);
    border-color: var(--color-olive);
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 4rem 3rem;
  }

  .featured-item-large {
    grid-column: 1 / -1;
  }

  /* Services Section */
  .services-section {
    background-color: var(--color-white);
  }

  .category-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    margin-top: 3.5rem;
  }

  .category-card {
    background-color: var(--color-white);
    padding: 3.5rem 2rem 3rem;
    border: 1px solid rgba(26, 26, 26, 0.03);
    transition: var(--transition-medium);
  }

  .category-card:hover {
    transform: translateY(-4px);
    border-color: var(--color-sand-dark);
    box-shadow: 0 10px 30px rgba(78, 94, 78, 0.04);
  }

  .category-icon-wrapper {
    color: var(--color-olive);
    margin-bottom: 2rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  .service-icon {
    width: 32px;
    height: 32px;
    stroke: var(--color-olive);
    stroke-width: 1.2;
    transition: var(--transition-medium);
  }

  .category-card:hover .service-icon {
    transform: scale(1.05);
    stroke: var(--color-olive-dark);
  }

  .category-title {
    font-family: var(--font-serif);
    font-size: 1.4rem;
    margin-bottom: 1rem;
    font-weight: 300;
    color: var(--color-charcoal);
  }

  .category-text {
    color: var(--color-charcoal-light);
    font-size: 0.95rem;
    margin-bottom: 0;
    line-height: 1.7;
    font-weight: 300;
  }

  /* Testimonials Section */
  .testimonials-section {
    background-color: var(--color-sand-base);
    border-top: 1px solid var(--color-sand-dark);
    border-bottom: 1px solid var(--color-sand-dark);
  }

  .testimonials-subtitle {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-limestone);
    margin-top: -1.25rem;
    margin-bottom: 0;
  }

  .testimonials-carousel {
    position: relative;
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-top: 3.5rem;
  }

  .testimonials-track {
    display: flex;
    gap: 2.5rem;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    padding: 0.5rem 0.25rem 1.5rem;
    margin-bottom: -1.5rem;
    scrollbar-width: none;
  }

  .testimonials-track::-webkit-scrollbar {
    display: none;
  }

  .testimonial-card {
    flex: 0 0 auto;
    scroll-snap-align: start;
    width: min(420px, 82vw);
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    background-color: var(--color-sand-light);
    border: 1px solid var(--color-sand-dark);
    padding: 2.25rem 2rem;
    position: relative;
  }

  .testimonial-card::before {
    content: '\201C';
    font-family: var(--font-serif);
    font-size: 4rem;
    line-height: 1;
    color: var(--color-sand-dark);
    position: absolute;
    top: 0.75rem;
    right: 1.25rem;
  }

  .testimonial-stars {
    color: var(--color-olive);
    font-size: 0.9rem;
    letter-spacing: 0.15em;
  }

  .testimonial-quote {
    font-family: var(--font-serif);
    font-size: 1.15rem;
    font-weight: 300;
    line-height: 1.7;
    color: var(--color-charcoal);
    font-style: italic;
    display: -webkit-box;
    -webkit-line-clamp: 7;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .testimonial-footer {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.5rem;
    margin-top: auto;
  }

  .testimonial-author {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-limestone);
  }

  .testimonial-link {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-olive);
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    transition: var(--transition-fast);
  }

  .testimonial-link .arrow {
    transition: transform 0.3s ease;
  }

  .testimonial-link:hover {
    color: var(--color-olive-dark);
  }

  .testimonial-link:hover .arrow {
    transform: translateX(3px);
  }

  .carousel-nav {
    flex: 0 0 auto;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    border: 1px solid var(--color-sand-dark);
    background: var(--color-sand-light);
    color: var(--color-charcoal);
    font-size: 1.1rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: var(--transition-fast);
  }

  .carousel-nav:hover {
    background: var(--color-olive);
    border-color: var(--color-olive);
    color: var(--color-white);
  }

  @media (max-width: 768px) {
    .carousel-nav {
      display: none;
    }
  }

  /* Contact Form Section */
  .contact-section {
    background-color: var(--color-sand-light);
  }

  .contact-header {
    margin-bottom: 5rem;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 6rem;
    align-items: start;
  }

  /* Form Styling */
  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  .form-group {
    display: flex;
    flex-direction: column-reverse;
    gap: 0.5rem;
    position: relative;
  }

  .form-group label {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-limestone);
    font-weight: 500;
    transition: var(--transition-fast);
  }

  .form-group input,
  .form-group textarea {
    border: none;
    border-bottom: 1px solid var(--color-sand-dark);
    background-color: transparent;
    padding: 0.75rem 0;
    font-family: var(--font-sans);
    font-size: 1rem;
    color: var(--color-charcoal);
    outline: none;
    transition: var(--transition-fast);
  }

  .form-group input:focus,
  .form-group textarea:focus {
    border-color: var(--color-olive);
  }

  .form-group input:focus + label,
  .form-group textarea:focus + label {
    color: var(--color-olive);
  }

  .submit-btn {
    align-self: flex-start;
    font-family: var(--font-sans);
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-white);
    background-color: var(--color-charcoal);
    border: none;
    padding: 1.25rem 2.5rem;
    cursor: pointer;
    transition: var(--transition-medium);
  }

  .submit-btn:hover {
    background-color: var(--color-olive);
  }

  .form-status {
    font-size: 0.9rem;
    margin-top: 1rem;
  }

  .form-status.success {
    color: var(--color-olive-dark);
  }

  /* Info Panel Styling */
  .contact-info-panel {
    background-color: var(--color-sand-base);
    padding: 4rem;
    border: 1px solid var(--color-sand-dark);
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  .info-block {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .info-label {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-olive-dark);
  }

  .info-text {
    font-size: 0.95rem;
    color: var(--color-charcoal-light);
    line-height: 1.6;
  }

  .info-link {
    color: var(--color-olive);
    transition: var(--transition-fast);
  }

  .info-link:hover {
    color: var(--color-olive-dark);
  }

  /* Workflow / Landscape Journey Section */
  :global(.workflow-section) {
    background-color: var(--color-sand-base);
    border-top: 1px solid var(--color-sand-dark);
    border-bottom: 1px solid var(--color-sand-dark);
    padding: 7rem 0;
  }

  :global(.workflow-container) {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  :global(.section-tag.center) {
    text-align: center;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.2rem;
    color: var(--color-olive);
    margin-bottom: 0.8rem;
    display: block;
    font-weight: 600;
  }

  :global(.section-heading.center) {
    text-align: center;
    font-family: var(--font-serif);
    font-size: 2.8rem;
    line-height: 1.2;
    margin-bottom: 1.5rem;
    font-weight: 300;
    color: var(--color-charcoal);
  }

  :global(.section-desc.center) {
    max-width: 700px;
    margin: 0 auto 3.5rem;
    text-align: center;
    color: var(--color-charcoal-light);
    font-size: 1.05rem;
    line-height: 1.7;
    font-weight: 300;
  }

  :global(.workflow-timeline) {
    margin-top: 4rem;
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  :global(.workflow-step) {
    display: grid;
    grid-template-columns: 80px 1fr;
    gap: 2rem;
    align-items: flex-start;
    border-bottom: 1px solid rgba(26, 26, 26, 0.05);
    padding-bottom: 2rem;
  }

  :global(.workflow-step:last-child) {
    border-bottom: none;
    padding-bottom: 0;
  }

  :global(.workflow-num) {
    font-family: var(--font-serif);
    font-size: 2.5rem;
    color: var(--color-olive);
    line-height: 1;
    font-weight: 300;
  }

  :global(.workflow-content h4) {
    font-family: var(--font-serif);
    font-size: 1.3rem;
    margin-bottom: 0.8rem;
    letter-spacing: 0.05rem;
    color: var(--color-charcoal);
    font-weight: 400;
  }

  :global(.paragraph) {
    color: var(--color-charcoal-light);
    font-size: 1.05rem;
    margin-bottom: 1.5rem;
    line-height: 1.8;
    font-weight: 300;
  }

  :global(.workflow-link) {
    color: var(--color-olive);
    font-weight: 500;
    text-decoration: underline;
    text-underline-offset: 4px;
    transition: var(--transition-fast);
  }

  :global(.workflow-link:hover) {
    color: var(--color-charcoal);
  }

  @media (max-width: 768px) {
    :global(.workflow-step) {
      grid-template-columns: 1fr;
      gap: 0.5rem;
    }
  }

  /* Keyframe Animations */
  /* Only animates transform (not opacity) on the hero title/eyebrow: this is
     the LCP-critical text on mobile (hero video is display:none there, so
     it's the largest painted element alongside the poster image). An
     opacity: 0 starting frame makes it unpainted at first paint, which is
     the likely cause of PageSpeed's NO_LCP on this page. */
  @keyframes fadeIn {
    from {
      transform: translateY(20px);
    }
    to {
      transform: translateY(0);
    }
  }

  @keyframes scrollDown {
    0% {
      top: -30%;
    }
    50% {
      top: 100%;
    }
    100% {
      top: 100%;
    }
  }

  /* Responsive styling */
  @media (max-width: 1024px) {
    .category-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 992px) {
    .spotlight-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .philosophy-grid {
      grid-template-columns: 1fr;
      gap: 4rem;
    }

    .philosophy-stat-col {
      flex-direction: row;
      border-left: none;
      border-top: 1px solid var(--color-sand-dark);
      padding-left: 0;
      padding-top: 3rem;
      gap: 4rem;
    }
    
    .projects-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .contact-grid {
      grid-template-columns: 1fr;
      gap: 5rem;
    }

    .contact-info-panel {
      padding: 3rem;
    }
  }

  @media (max-width: 768px) {
    .hero {
      align-items: flex-end;
      padding-bottom: 6rem;
      height: calc(100vh - 4px);
    }

    .hero-title {
      animation: none;
    }

    .hero-eyebrow {
      animation: none;
    }
    
    .hero-video {
      display: none;
    }

    .scroll-indicator {
      display: none;
    }

    .featured-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 1.5rem;
      margin-bottom: 3rem;
    }

    .category-grid {
      grid-template-columns: 1fr;
    }
    
  }
</style>
